# connect-rate-automation
This script automates the consolidation of Connect Rate Excel reports.


# Connect Rate Dashboard Processor

This script automates the consolidation of Connect Rate Excel reports.

## Setup
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Place raw Excel files in the `data/raw/` folder.
4. Run the script: `python src/process_data.py`
